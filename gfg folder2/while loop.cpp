#include<iostream>
using namespace std;
int main(){
    while(n)//it means till n is true i.e it is greater than 0 while(5)it runs for n=5,4,3,2,1 but inside loop n=5,4,3,2,1,0(when inside while loop we had declare n--)
    int m=6;
    while(m--)//while loop ke andar m=5,4,3,2,1,0 becoz start main hi m-1 hogya
    /*while(q--){
        n=n+q;
        cout<<n;
    }
    here q value will change inside loop also as outer in loop is has been decrement continiously
    */

   /* while(n>0){//yeh n ko check karega jab tak voh >0 h but voh idhar n ko change nhi karega
       q=q+n;
       n--;// n yahan pe change karega apni value poore while loop ke liye :)
   }
   */
  // while(n>0) is same as while(n)

 /* #include<iostream>
using namespace std;
int main()
{
    int n = 5;
    int s = 0;
  
    while(n>0)
    {
        s = s + n;
        //  cout << n << ' ';
        --n;
    }
    // cout << n << endl;
    cout << s;
    return 0;
}
*/

}